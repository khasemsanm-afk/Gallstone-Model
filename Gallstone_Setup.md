# คู่มือการติดตั้งระบบ AI นิ่วในถุงน้ำดี บนคอมพิวเตอร์คลินิก (ไม่มีการ์ดจอ)
**ชื่อโปรเจกต์:** MedGemma-Gallstone (CPU Inference via Ollama)

---

## ขั้นที่ 1: ติดตั้งโปรแกรม Ollama (ตัวรัน AI บน CPU)
1. เข้าไปที่เว็บไซต์: [https://ollama.com/download](https://ollama.com/download)
2. คลิกดาวน์โหลดสำหรับ **Windows**
3. เมื่อดาวน์โหลดเสร็จ ให้ดับเบิลคลิกไฟล์ `OllamaSetup.exe` แล้วกด Install ติดตั้งตามปกติ
4. หากติดตั้งสำเร็จ จะมีไอคอนรูป **ตัวลามะ** ปรากฏขึ้นที่มุมขวาล่างของหน้าจอคอมพิวเตอร์ (แถวๆ นาฬิกา)

---

## ขั้นที่ 2: สร้างไฟล์ตั้งค่า (Modelfile)
เราต้องเขียนกฎบอก Ollama ว่าให้โหลดไฟล์นี้และใช้เทมเพลตการคุยแบบไหน

1. เปิดโฟลเดอร์ `D:\Work\finetune\qlora_gallstone_v2\models\model_ollama_gguf`
2. หาไฟล์ที่ลงท้ายด้วย `.gguf` (เช่น `medgemma-1.5-4b-it.Q4_K_M.gguf`) แล้วก๊อปปี้ชื่อไฟล์นั้นไว้
3. คลิกขวาในโฟลเดอร์นั้น เลือก **New > Text Document**
4. ตั้งชื่อไฟล์ว่า **`Modelfile`** (ระวังอย่าให้มีนามสกุล .txt ต่อท้าย)
5. เปิดไฟล์ `Modelfile` ด้วยโปรแกรม Notepad แล้วก๊อปปี้ข้อความด้านล่างนี้ไปวาง:

```text
FROM ./medgemma-1.5-4b-it.Q4_K_M.gguf

TEMPLATE """<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
"""
```
*(สำคัญ: ตรงคำว่า `medgemma-1.5-4b-it.Q4_K_M.gguf` ต้องแก้ให้ตรงกับชื่อไฟล์ GGUF ของจริงที่คุณหมอมีในโฟลเดอร์นะครับ)*
6. กด **Save** แล้วปิด Notepad

---

## ขั้นที่ 3: นำเข้าสมอง AI สู่ระบบ Ollama
1. เปิดโปรแกรม **Windows PowerShell**
2. พิมพ์คำสั่งเข้าไปในโฟลเดอร์โมเดล:
```powershell
cd D:\Work\finetune\qlora_gallstone_v2\models\model_ollama_gguf
```
3. พิมพ์คำสั่งดึงโมเดลเข้าสู่ฐานข้อมูลของ Ollama (ใช้เวลาไม่กี่วินาที):
```powershell
ollama create medgemma-gallstone -f Modelfile
```

---

## ขั้นที่ 4: ทดสอบใช้งานจริง!
ตอนนี้ AI เข้าไปฝังอยู่ในเครื่องคอมพิวเตอร์เรียบร้อยแล้วครับ! 

**วิธีที่ 1: ทดสอบคุยผ่านหน้าจอดำ (Terminal)**
พิมพ์คำสั่งนี้ใน PowerShell เพื่อเริ่มคุย:
```powershell
ollama run medgemma-gallstone
```
พอมันขึ้นเครื่องหมาย `>>>` สามารถก๊อปปี้ประวัติคนไข้ แปะลงไปแล้วกด Enter เพื่อดูผลลัพธ์การสกัดขนาดนิ่วได้เลย (ถ้าต้องการออกจากการคุย ให้พิมพ์ `/bye`)

**วิธีที่ 2: เชื่อมต่อผ่าน Backend (ทำระบบอัตโนมัติ)**
เมื่อติดตั้งเสร็จแล้ว Backend FastAPI (ที่เขียนไว้ในไฟล์ `system_architecture_guide.md`) จะสามารถทำงานเชื่อมต่อกับ Ollama ในเครื่องได้ทันทีแบบ 100% Offline ครับ!

---

## ขั้นที่ 5: วิธีอัปเดตหรือเปลี่ยนโมเดล (ถอดของเก่า ใส่ของใหม่)
ถ้าในอนาคตต้องการ Train ข้อมูลเพิ่ม (เป็นเวอร์ชันใหม่) แล้วแปลงไฟล์เป็น GGUF ตัวใหม่มา สามารถ "ถอดสมองเก่าทิ้ง แล้วเสียบสมองใหม่" ได้ง่ายๆ ดังนี้:

**วิธีอัปเดตเวอร์ชันใหม่ทับตัวเดิม:**
1. เปิด PowerShell พิมพ์คำสั่งลบโมเดลตัวเก่าทิ้ง:
   ```powershell
   ollama rm medgemma-gallstone
   ```
2. เอาไฟล์ `.gguf` ตัวใหม่มาวางในโฟลเดอร์
3. เปิดไฟล์ `Modelfile` ขึ้นมาแก้ชื่อไฟล์ตรงบรรทัด `FROM ./...` ให้ตรงกับชื่อไฟล์ใหม่
4. สั่งติดตั้งใหม่ด้วยคำสั่งเดิม:
   ```powershell
   ollama create medgemma-gallstone -f Modelfile
   ```

**วิธีสลับใช้หลายโรคพร้อมกัน (ไม่ต้องลบของเก่า):**
ถ้ามีหลายโมเดล เช่น นิ่ว (gallstone) และ เบาหวาน (diabetes) ให้สร้างตั้งชื่อแยกกันตอนรันคำสั่ง create เช่น:
* `ollama create medgemma-gallstone -f Modelfile_1`
* `ollama create medgemma-diabetes -f Modelfile_2`

เวลาใช้งานจริงในโค้ด Backend API แค่เปลี่ยนชื่อตรงคำว่า `"model": "..."` ระบบ Ollama ก็จะสลับสมองสลับโรคให้เสี้ยววินาทีแบบอัตโนมัติครับ!

---
**เสร็จสมบูรณ์!** ตอนนี้คอมพิวเตอร์ของคุณมี AI ส่วนตัวระดับโรงพยาบาลโดยไม่ต้องง้อการ์ดจอ และไม่ต้องต่ออินเทอร์เน็ตอีกต่อไปครับ!
